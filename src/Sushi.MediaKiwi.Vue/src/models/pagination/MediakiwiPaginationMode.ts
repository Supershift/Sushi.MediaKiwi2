import { VuetifyPaginationMode } from "./VuetifyPaginationMode";

/**
 * The pagination mode.
 * @extends VuetifyPaginationMode
 * @property {string} controls The pagination is controlled by arrow controls.
 */
export type MediakiwiPaginationMode = VuetifyPaginationMode | "controls";
