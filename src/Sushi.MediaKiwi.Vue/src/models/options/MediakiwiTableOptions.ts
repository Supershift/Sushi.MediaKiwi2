export type MediakiwiTableOptions = {
  /** 'Tracks' the item the user viewed when changing pageSizes in table, when true calculates pageIndex instead of resetting pageIndex to 0 */
  pageTracking?: boolean;
};
