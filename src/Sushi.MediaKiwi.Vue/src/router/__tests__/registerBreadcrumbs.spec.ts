import { describe, it, expect, beforeEach, vi } from "vitest";
import { container } from "tsyringe";

//TODO: write a test to prove that registerBreadcrumbs work
describe("Breadcrumbs", () => {
    beforeEach(() => {
      container.reset();
    });
    it("Should register given breadcrumbs", () => {
      // arrange
  
      // act
  
      // assert

    });
})